# SUTRA-GEO production backend scaffold

This folder is a production-starting point, not a deployed backend. The current GitHub/Vercel prototype remains client-side.

Planned services: FastAPI, PostgreSQL/PostGIS, authenticated journey events, server-side quest completion, moderation roles, rate limits and an LLM proxy.

Run only after installing the dependencies and setting secrets.
